# TemplateService.createTemplate 개선 제안 (검토용)

본 문서는 `TemplateService.createTemplate` 메서드의 성능, 동시성, 유지보수 측면에서의 잠재적 리스크와 그에 대한 개선 방안을 정리한 것입니다.

## 1. 현재 구조의 문제점 분석

### ① N+1 Insert 발생 (성능 저하)
- **현상**: 운동 목록(`Exercises`) 루프 내에서 `insertExercise`와 `insertRelation`을 개별적으로 호출.
- **리스크**: 운동이 10개라면 [1(Master) + 2 * 10(Detail)] = 총 21번의 쿼리가 발생. 운동 수가 많아질수록 DB 커넥션 점유 시간이 길어지고 시스템 전체 성능이 저하됨.

### ② SORT_ORDER의 동시성 이슈 (Race Condition)
- **현상**: 매퍼 XML 내의 서브쿼리(`SELECT IFNULL(MAX(SORT_ORDER), 0) + 1 ...`)로 순번 생성.
- **리스크**: 다수의 사용자가 동시에 등록을 시도할 경우, 동일한 `MAX` 값을 조회하게 되어 중복된 `SORT_ORDER`가 생성되거나 데이터 무결성 오류(PK/Unique 제약 위반)가 발생할 수 있음.

### ③ 비즈니스 로직의 파편화 및 매핑 복잡도
- **현상**: `request` 객체에서 새로운 `tplDto`로 필드를 일일이 복사(`set`)하여 처리.
- **리스크**: 필드 추가 시 서비스 로직을 매번 수정해야 하며, 누락될 가능성이 높음. 유지보수 비용이 증가함.

### ④ 입력값 검증(Validation) 부재
- **현상**: 필수값(`tplName`, `exercises` 등)에 대한 체크 없이 DB Insert 시도.
- **리스크**: 비정상적인 데이터가 저장되거나 불필요한 DB 예외 로그가 대량 발생함.

---

## 2. 기술적 개선 제안

### ① Batch Insert 도입 (MyBatis <foreach> 활용)
- 단일 호출로 여러 건의 데이터를 한 번에 Insert 하도록 매퍼와 서비스 로직 개선.
- 네트워크 오버헤드를 최소화하고 트랜잭션 처리 속도 향상.

### ② SORT_ORDER 생성 전략 변경
- DB 서브쿼리 대신 서비스단에서 루프 인덱스를 활용하거나, 전용 시퀀스/채번 테이블 사용 고려.
- 동시성 제어가 필요한 경우 데이터베이스 락(Lock)이나 유니크 인덱스를 통한 정교한 제어 필요.

### ③ 객체 매핑 전략 개선
- `request` DTO 자체를 매퍼에 전달하거나, `MapStruct` 또는 `BeanUtils.copyProperties` 같은 유틸리티를 사용하여 코드 가독성 향상.

---

## 3. 기대 효과
- **성능 향상**: 대량의 데이터 처리 시 기존 대비 쿼리 실행 횟수를 80% 이상 감소시킬 수 있음.
- **안정성 확보**: 동시성 문제를 해결하여 데이터 정합성 보장.
- **유지보수 용이성**: 비즈니스 로직이 단순화되어 코드 가독성이 좋아지고 향후 확장성이 확보됨.
